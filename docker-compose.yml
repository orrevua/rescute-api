services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: rescute
      POSTGRES_USER: rescute
      POSTGRES_PASSWORD: rescute
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
